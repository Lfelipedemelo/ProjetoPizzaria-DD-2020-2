package br.com.pizza;

import java.io.FileNotFoundException;

public class GeradorPanilha {

	/*public void gerarPlanilhaProdutos(list<Produto> produtos, String caminho) {
		String [] colunasDaPlanilha = { "Nome", "Endereco", "Telefone"};
				
				HSSFWorkbook planilha = new HDDFWorkbook();
			
				HSSFSheet abaPlanilha.createSheet("Clientes");
						
						Row headerRow = abaPlanilha.createRow(0);"
						
						for (int i=0; i < colunasDaPlanilha.length; i++ ) {
							Cell cell = headerRow.createCell(i);
							cell.setCellValue(colunasDaPlanilha[i]);
						}
		
						int rowNum = 1;
						for (Produto prod : produtos) {
							Row novaLinha = abaPlanilha.createRow(rowNum++);
							
							novaLinha.createCell(0).setCellValue(prod.getNome());
							novaLinha.createCell(1).setCellValue(prod.getNome());
							novaLinha.createCell(2).setCellValue(prod.getNome());
							novaLinha.createCell(3).setCellValue(prod.getNome());
						}
						
						for (int i = 0; i < colunaDaPlanilha.length; i++) {
							abaPlanilha.autoSizeColumn(i);
						}
						FileOutputStream fileout = null;
						try {
							fileOut = new  FileOutputStream(caminho + ".xls");
							planilha.write(fileOut);
						} catch (FileNotFoundException e) {
							e.printStackTrace();
						} catch
					
	}
	
*/	
}